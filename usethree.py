import sys
import stdio

# Write a proper sentence with three names in the reverse order they are given

stdio.write('Hi ')
stdio.write(sys.argv[3])
stdio.write(', ')
stdio.write(sys.argv[2])
stdio.write(', and ')
stdio.write(sys.argv[1])
